<UserControl
    x:Class="PasswordSaver.UcDataItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PasswordSaver"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="650"
    HorizontalAlignment="Stretch">
    <Grid Name="grdDataItem" HorizontalAlignment="Stretch">
        <StackPanel Margin="5,5,5,30" HorizontalAlignment="Stretch">
            <TextBlock>网站</TextBlock>
            <TextBlock Text="{Binding WebSite}"/>
            <TextBlock>Account</TextBlock>
            <TextBlock Text="{Binding Account}"/>
            <Button Name="btnDetail" Click="btnDetail_Click" Content="Detail" HorizontalAlignment="Right"/>
            <StackPanel Name="stkpDetail"
                        Visibility="Collapsed">
                <TextBlock>Pwd</TextBlock>
                <TextBlock Text="{Binding Pwd}"/>
                <TextBlock>Note</TextBlock>
                <TextBlock Text="{Binding Note}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
